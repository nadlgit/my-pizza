import { render, screen } from '@testing-library/react';
import '@testing-library/jest-dom/extend-expect';

import { OrderStart } from './order-start';
import { PIZZA_BASES } from 'data/bases';
import { PIZZA_INGREDIENTS } from 'data/ingredients';

describe.skip('OrderStart component', () => {
  it.skip('should have all bases visible and not disabled', () => {});

  it.skip('should have all ingredients visible and not disabled', () => {});

  it.skip('should have cancel and continue buttons visible and not disabled', () => {});

  it.skip('should have props order base and ingredients initially selected', () => {});

  it.skip('should have price updated when selection changes', () => {});
});
